services:
  app:
    build: .
    container_name: money-manager
    ports:
      - "6080:6080"
    volumes:
      - ./data:/app/data
    environment:
      - DISPLAY=:99
      - KIVY_NO_ARGS=1
      - PYTHONUNBUFFERED=1
